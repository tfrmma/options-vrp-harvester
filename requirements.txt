# core
httpx==0.28.1
websockets==15.0.1
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
python-dotenv==1.2.2
aiosqlite==0.22.1

# display
rich==15.0.0
loguru==0.7.3

# web3 / signing
eth-account==0.13.7
web3==7.16.0

# live trading only -- pin tightly, EIP-712 serialization breaks silently on version mismatch
# derive-action-signing==<pin after first install>
# install from: pip install git+https://github.com/derivexyz/v2-action-signing-python
